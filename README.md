# URL Health Checker (CLI)

A simple Python CLI tool to check website availability and response time.

This project is shared as a small **New Year release** for learning, testing, and basic monitoring purposes.

---

## 🚀 Features
- Check HTTP status code
- Measure response time
- Handle timeout and common request errors
- Simple and beginner-friendly CLI output

---

## 📦 Requirements
- Python 3.8+
- `requests` library

clone: git clone https://github.com/RenOpSo2/new-year-2026-url-checker

Install dependencies:
```bash
pip install -r requirements.txt
